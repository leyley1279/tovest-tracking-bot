version: "3.9"

services:
  tovest-bot:
    build: .
    container_name: tovest_tracking_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # Persist the SQLite database, invite links cache, and logs outside the container
      - ./data:/app/data
      - ./logs:/app/logs
    environment:
      - DB_PATH=/app/data/tracking.db
